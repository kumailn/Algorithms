def longestCommonPrefix(strs):
    """
    :type strs: List[str]
    :rtype: str
    """
    for (string in strs):
      for i in range(len(string)[::-1]:
        
        if(string[:i] in )

        

print(longestCommonPrefix(["flower","flow","flight"]))